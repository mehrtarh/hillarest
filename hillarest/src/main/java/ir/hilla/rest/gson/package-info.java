/**
 * This package provides the {@link ir.hilla.rest.gson.HillaGson} class to convert Json to Java and
 * vice-versa.
 *
 * <p>The primary class to use is {@link ir.hilla.rest.gson.HillaGson} which can be constructed with
 * {@code new HillaGson()} (using default settings) or by using {@link ir.hilla.rest.gson.HillaGsonBuilder}
 * (to configure various options such as using versioning and so on).</p>
 *
 * @author Inderjeet Singh, Joel Leitch
 */
package ir.hilla.rest.gson;